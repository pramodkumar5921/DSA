Int fun( string s1, string s2, int m, int n) {
// base case
If (m==O or n==O) return 0;
// recursive case
if(s1[m-1]==s2[n-1]) 
return 1+fun(s1, s2, m-1, n-1) ;
else{
Max (fun(s1, s2, m-1, n),fun(s1, s2, m, n-1))
;
}
